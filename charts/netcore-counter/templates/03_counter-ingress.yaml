apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: {{ template "netcore-counter.fullname" . }}
spec:
  rules:
  - host: {{ template "netcore-counter.fullname" . }}.35.233.36.7.nip.io
    http:
      paths:
      - backend:
          serviceName: {{ template "netcore-counter.fullname" . }}
          servicePort: 5000
