apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: netcore-counter
spec:
  rules:
  - host: netcore-counter.35.195.192.216.nip.io
    http:
      paths:
      - backend:
          serviceName: netcore-counter
          servicePort: 5000
