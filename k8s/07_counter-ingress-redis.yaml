apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: netcore-counter-redis
spec:
  rules:
  - host: netcore-counter-redis.35.195.12.103.nip.io
    http:
      paths:
      - backend:
          serviceName: netcore-counter-redis
          servicePort: 5000
